# Owner: Hiba
# Vault bootstrap values only.
# App secrets such as DB, Redis, MinIO, service URLs, JWT signing keys,
# and API credentials must be resolved from Vault.

VAULT_ADDR=http://vault:8200
VAULT_TOKEN=replace-with-vault-token
VAULT_APP_SECRET_PATH=secret/data/concierge/app
